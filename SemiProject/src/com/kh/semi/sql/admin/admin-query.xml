<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

<!-- 전체 회원 수 조회-->
<entry key="getListCount">
SELECT COUNT(*) FROM MEMBER
WHERE MEM_STATUS = 'Y'
</entry>

<!-- 이따 조인할 애 -->
<entry key="selectMemberList">
SELECT * FROM
	(SELECT ROWNUM RNUM, V.*
	FROM
	(SELECT * FROM MEMBER WHERE MEM_STATUS = 'Y' ORDER BY MEM_NO DESC) V)
WHERE MEM_GRADE = 'G' AND
RNUM BETWEEN ? AND ?
</entry>

<entry key="addSubList">
SELECT LISTAGG(SERV_NM, ', ') WITHIN GROUP (ORDER BY SERV_NM) FROM ADMIN_VIEW WHERE MEM_NO = ?

</entry>

</properties>
